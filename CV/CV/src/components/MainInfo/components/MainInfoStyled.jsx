import { styled } from "styled-components";
import { MainInfoMixin } from "../Mixin/MainInfoMixin";

export const MainInfoStyled = styled.div`
    ${MainInfoMixin}
    background: #FFF;
`;
export const DarkMainInfoStyled = styled.div`
    ${MainInfoMixin}
    background: #232339;
`;
